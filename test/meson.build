# Define the project
project('test_project', 'c',
  version: '0.1',
  default_options: ['warning_level=3', 'buildtype=debugoptimized'])

# Specify the source file
executable('virtiofs-test', 'test.c', install: true)
